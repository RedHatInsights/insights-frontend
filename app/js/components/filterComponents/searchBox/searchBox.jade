span
    input(
      type='text',
      autofocus
      placeholder='{{placeholder}}',
      ng-keypress='keyPressed($event)',
      ng-model='model',
      ng-change='change()',
      ng-class="{'search-box form-control': (isFullLength !== undefined)}"
    )
