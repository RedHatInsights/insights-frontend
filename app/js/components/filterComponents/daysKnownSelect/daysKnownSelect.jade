.filter-group.filter-rhsa-severity
  label(translate) Days Known
  md-menu(md-offset="0 38")
    md-button.button-filter(
      type='button',
      data-toggle='dropdown',
      aria-haspopup='true',
      aria-expanded='false',
      ng-click='$mdMenu.open()')
      span {{selected.title | translate}}
      span.caret
    md-menu-content
      md-menu-item(ng-repeat='(key, option) in options')
        a(ng-click='select(key)') {{option.title | translate}}
